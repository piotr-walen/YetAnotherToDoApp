{
    "scripts": {
        "heroku-postbuild": "cd server && npm install && ls node_modules && npm run tsc && cd ../client && npm install && npm run build",
        "start": "node server/dist/index.js"
    }
}
